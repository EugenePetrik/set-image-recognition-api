# Test Environment Configuration
TF_ENVIRONMENT=dev
AWS_REGION=us-east-1
AWS_PROFILE=default

# Test Mode (unit, integration, all)
TEST_MODE=unit

# Coverage Settings
COVERAGE_THRESHOLD=80

# Terraform Settings
TERRAFORM_DIR=../tf-dev
